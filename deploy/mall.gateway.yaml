apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: mall-gateway
  name: mall-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mall-gateway
  template:
    metadata:
      labels:
        app: mall-gateway
    spec:
      containers:
      - image: jamesxpenn/mall-gateway:v1
        name: mall-gateway
        ports:
        - containerPort: 9001
        env:
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
             name: secret-mysql
             key: username
        - name: MYSQL_PWD
          valueFrom:
            secretKeyRef:
             name: secret-mysql
             key: password             
        - name: MYSQL_HOST
          valueFrom:
            secretKeyRef:
             name: secret-mysql
             key: host                 
        - name: MYSQL_PORT
          valueFrom:
            secretKeyRef:
             name: secret-mysql
             key: port                 
        - name: NACOS_HOST
          valueFrom:
            secretKeyRef:
             name: secret-nacos
             key: host                     
        - name: NACOS_PORT
          valueFrom:
            secretKeyRef:
             name: secret-nacos
             key: port
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: mall-gateway
  name: mall-gateway
spec:
  ports:
  - port: 9001
    protocol: TCP
    targetPort: 9001
  selector:
    app: mall-gateway             
